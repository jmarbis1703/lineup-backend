# Web framework
fastapi==0.135.1
uvicorn[standard]==0.42.0

# Database
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.18.4

# Settings / validation
pydantic-settings==2.13.1
email-validator==2.3.0

# Auth
python-jose[cryptography]==3.5.0
svix==1.88.0
cachetools==7.0.5

# Background workers
celery[redis]==5.6.2
redis==6.4.0

# Rate limiting (SEC-09)
slowapi>=0.1.9        # not installed in current venv — pin pending first install
limits[redis]>=3.7.0  # not installed in current venv — pin pending first install

# HTTP client
httpx==0.28.1

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0

# --- Dev/audit tools ---
pip-audit==2.10.0

# ----- email val -----
email-validator==2.3.0
